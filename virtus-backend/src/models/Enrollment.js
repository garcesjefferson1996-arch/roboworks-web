// [DESCARTADO] Este modelo era para Firestore. Se decidio seguir con MySQL.
// Las inscripciones viven en la tabla class_students (ver db/schema.sql) y se
// manejan directamente en adminRoutes.js / classRoutes.js.
module.exports = {};
